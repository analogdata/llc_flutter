void main(){
  const pi = 3.14;
  print(pi);

  const name = "Rajath";
  print(name);

  // Why const over final ?
  // 1. const are compile time constants
  // 2. const are faster than final
  // 3. const are immutable

  // We can use const > final > var. It'll optimize the code to run faster.
}