void main() {
  int age = 18;

  // Conditional if-else Statement
  if (age >= 18) {
    print("You can vote");
  } else {
    print("You cannot vote");
  }

  // Ternary Operator: It is a short way of writing above if-else
  print(age >= 18 ? "You can vote" : "You cannot vote");
}